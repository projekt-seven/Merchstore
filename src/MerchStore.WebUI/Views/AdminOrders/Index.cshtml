@model List<MerchStore.Application.DTOs.OrderListItemDto>

@{
    ViewData["Title"] = "Orders";
}

<h1 class="mb-4">Orders</h1>

<a asp-action="Create" class="btn btn-success mb-3">➕ Create new order</a>

<table class="table table-striped table-bordered">
    <thead class="table-dark">
        <tr>
            <th>Order ID</th>
            <th>Customer</th>
            <th>Total (SEK)</th>
            <th>Date</th>
            <th>Status</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var order in Model)
        {
            <tr>
                <td>@order.Id</td>
                <td>@order.CustomerFullName</td>
                <td>@order.TotalAmount.ToString("0.00")</td>
                <td>@order.OrderDate.ToShortDateString()</td>
                <td>@order.Status</td>
                <td>
                    <a asp-action="Details" asp-route-id="@order.Id" class="btn btn-info btn-sm">View</a>
                    <a asp-action="Edit" asp-route-id="@order.Id" class="btn btn-warning btn-sm">Edit</a>
                    <form asp-action="Delete" asp-route-id="@order.Id" method="get" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this order?');">
                        <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
